import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
      Sadece giriş yapmış olan kullanıcılar burayı görebilir!
    </Typography>
  )
}
